function capToFront(letter){
    let upper = [];
    for(let i = 0;i<= letter.lenght;i++){
        if(letter == letter.toUpperCase()){
          upper.push(letter);
        console.log(upper);
        }
    }
}

console.log(capToFront("hApPy"));